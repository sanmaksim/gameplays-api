{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build",
        "type": "shell",
        "command": "dotnet",
        "args": [
          "build",
          "${workspaceFolder}/GameplaysBackend/GameplaysBackend.csproj"
        ],
        "options": {
          "cwd": "${workspaceFolder}/GameplaysBackend"
        },
        "problemMatcher": "$msCompile"
      }
    ]
}
